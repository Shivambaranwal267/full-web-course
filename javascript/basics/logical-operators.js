// logical operators

// && (Logial AND)
// || (Logial OR)
// !  (Logial NOT)


// //(Logial AND)
 /*const age =19;
  if(age > 19 && age < 40) {
        console.log('You can drive');
   }
   else {
       console.log('You cannot drive');
   }
*/
//(Logial OR)

 /* const age =59;
  const isSkilled = true;

 if((age > 19 && age < 40) || isSkilled)  {
       console.log('You can drive');
  }
  else {
      console.log('You cannot drive');
  }*/


  // (logical NOT)
  
//   const age =20;
 

 /*
  if(!(age > 19) )  {
       console.log('You can drive');
  }
  else {
      console.log('You cannot drive');
  }
*/